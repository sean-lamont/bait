# @package _global_
defaults:
  - holstep
  - /data_type/gnn@_here_

exp_config:
  name: bag_of_words

model_config:
  model_type: formula-net-edges
  model_attributes:
    embedding_dim: 256
    gnn_layers: 0
    batch_norm: True