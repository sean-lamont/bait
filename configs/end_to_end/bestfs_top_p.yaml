# @package _global_
defaults:
  - end_to_end_defaults
  - env_config/leandojo_minif2f
  - tac_model/lean_reprover
  - search_model/diversity

exp_config:
  name: bestfs_top_p
  experiment: minif2f

logging_config:
  project: minif2f
  offline: false

search_model.diversity_config.use_model: false

tac_model.gpu_per_process: 0.225
tac_model.cpu_per_process: 0.125

hydra:
  sweeper:
    params:
      search_model.diversity_config.p: 0.75, 0.9, 0.95
