defaults:
  - /base/data_config@_here_
  - /base/optimiser_config@_here_
  - /base/logging_config@_here_
  - /base/exp_config@_here_

epochs: 10

exp_config:
  experiment: premise_selection

limit_val_batches: False

val_frequency: 2048

batch_size: 32

model_config:
  model_type: ???
  model_attributes: ???

# Whether to run a test following the fit method of the trainer
test_on_finish: True
# If loading a checkpoint, whether to train or test
test_checkpoint: False