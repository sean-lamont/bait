defaults:
  - premise_selection

data_config:
  data_options:
    db: 'hol4_original_ast'

epochs: 10

exp_config:
  name: bow

model_config:
  model_type: formula-net-edges
  model_attributes:
    vocab_size: 2200
    embedding_dim: 256
    gnn_layers: 0
    batch_norm: False

logging_config:
  project: hol4_premise_pretrain
