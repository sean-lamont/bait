# @package _global_
defaults:
  - holist_supervised
  - /data_type/sequence@_here_

exp_config:
  name: transformer

model_config:
  model_type: holist_transformer
  model_attributes:
    dropout: 0.2
    embedding_dim: 128
    num_layers: 4
    num_heads: 4
    dim_feedforward: 256