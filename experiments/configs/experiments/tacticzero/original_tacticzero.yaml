# @package _global_
defaults:
  - tactic_zero

logging_config:
  offline: False

max_steps: 50
data_config:
  type: 'fixed'
  data_options:
    filter: []

exp_config:
  name: vanilla_full_fringe_50

model_config:
  model_type: 'fixed_autoencoder'
  model_attributes:
    checkpoint_path: 'experiments/tacticzero/model_checkpoints/tacticzero_autoencoder'
    embedding_dim: 256