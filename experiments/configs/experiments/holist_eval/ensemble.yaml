# @package _global_
defaults:
  - holist_eval
  - /data_type/ensemble@_here_

exp_config:
  name: ensemble

model_config:
  model_type: ensemble_holist
  model_attributes:
    gnn_layers: 12
    dropout: 0.2
    embedding_dim: 128
    num_layers: 4
    num_heads: 4
    dim_feedforward: 256