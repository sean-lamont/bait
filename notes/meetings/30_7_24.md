# PhD Update 30/07/2024

## DPP
- Fixed some bugs with DPP model
- Updated DPP model to filter after running in environment
  - Costs extra time to run every tactic, but prevents filtering out useful tactics
  - Not clear whether this is better yet
- Started by filtering maximum of (k, trace(sim_matrix))
  - Expected value of a sample from DPP is trace(sim_matrix)
  - More similar matrices give lower trace, and vice versa
  - Testing with k = 2, 8, 16
    - k = 16 works best so far, doing slightly better than normal bestfs 


- Problem: Looking at the traces, trace(sim_matrix) is quite small (and doesn't actually vary much),
 so we're filtering too many tactics (without the minimum)
- Hence higher strict minimum seems to work better

- Dynamic number of tactics filtered
  - Rather than trace(sim), use top-p similar to nucleus sampling with the eigenvalues of K_sim
    - Use K_sim rather than K so we ignore the scores, get an idea of how many diverse tactics we have
     
## Experiments
  - Hyperparameter sweep for:
    - p controlling the number of tactics filtered
    - temperature for the scores
  - Swap to miniF2F
    - Smaller set, makes it faster to sweep and compare models, common benchmark
  - How to evaluate?
    - End-to-end? Include seq2seq finetuning, and further tuning of the tactic model with new transitions
      - Which models to compare? Vanilla BestFS vs DPP with best hyperparam settings?

## Story
- 1: We can predict environment dynamics, and embed them into a single vector
      - BLEU/ROGUE scores
      - Autorater results
    - Prediction without tactic < prediction with isolated tactic < prediction with tactic in context
        - (Also should test prediction with all tactic tokens in context)
         
- 2: We can use this to filter tactics in a more intelligent way
    - Motivation: Large branching factor, similar tactics should be pruned
        - DPP gives elegant way to do this, trading off diversity and score
    - Evaluation:
        - Hyperparameter sweep
        - Evaluation on miniF2F/novel premises
        - Comparison with vanilla bestfs
        - Comparison with autoencoder
        - Comparison with diverse beam search
        - Comparison with top-k logprobs
        - End-to-end evaluation of bestfs vs DPP model
         
- Also have subgoal separation giving a decent boost in performance..
 
## TODO
- Set up miniF2F 
- Run hyperparameter sweep and comparisons on validation set of miniF2F
- (later) test end-to-end version on miniF2F
