# PhD Update 16/7/24

## Update
### DPP
- Idea:
  - Generate K samples from fast LLM
  - Filter to k' < K using DPP.
    - DPP applied using tactic embeddings as feature vectors, where the embeddings are generated to capture
    the semantics of the tactic in the environment transition
    - The magnitude of the vectors are typically normalised, then scaled by a 'quality' score. For our case,
    this naturally is the logprob from the LLM. In this way, a tradeoff between quality and diverse tactics are chosen
- Currently training tactic transition model
  - Algo 1: Embed tactic with tac_embedding model and then concatenate with goal embedding, feeding this to decoder to
    predict the environment outcome
  - Algo 2: Run encoder with goal + tactic, take tactic tokens, mean pool, concatenate this embedding with the original
  goal embeddings, run encoder-decoder to predict environment transition
    - Had to restart experiment, at first I was taking the goal tokens after the first encoder (which doesn't bottleneck 
    the flow into the tactic vector)
  - From looking at some of the predictions, seems like it can learn tactic behaviours fairly well
  - Still need to do a more detailed comparison between approaches (e.g. LLM AutoRanker)
    - Chain of thought (explain A, explain B, then compare, then give score, then parse format with regex )
- Implemented DPP model given a tactic vector, and integrated into end-to-end experiments
  - Need to test this once we have a trained transition model

## TODO
- Finish tactic transition training
- Start evaluating different transition models (autoencoder, single tactic without goal attention, tactic + goal attn)
- Run DPP in end-to-end

