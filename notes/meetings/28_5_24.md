# PhD Update 28/5/24


- Running on cluster

- Compared sugboal splitting vs single state
  - Improved, but highlights an issue going forward

- Human replay data still running

## Timeout vs limited node expansions
- Better hardware will prove more given the same timeout
- Other possibility is to limit by number of tactics/nodes expanded
  - Problematic as well because some goals result in very time consuming tactics
    - Penalise tactic time when finetuning? E.g. reward scaled by tactic time 
  - If we limit to a high # tactics/nodes expanded, these nodes will take much longer to run 
  - On the other hand, if we keep a low # tactics/nodes, then most proofs won't be found
- For now, keep all comparisons on the same hardware?
  - Evaluate with A6000's, and prototype on 4090 / cluster

## Different performance of different hardware
- The time to execute a single tactic can be much longer on some hardware than others
- Further support to keep hardware constant 

## Error logging
- Sometimes error in environment initialisation, leading to the proof being ignored 
  - Added more logging
- Comparisons should only consider goals which were actually attempted

## LLM data generation
- Seem to do much better than last time 
- Good for augmenting/transforming a given goal
  - Take proven goals, get model to find siblings which are similar
  - Take error/highly explored nodes, get model to find goals which are similar and different (to score lowly/highly respectively)
  - Get model to generate new goals which are small changes to the original

# TODO
- Use human data and data from first BestFS to train pair model
- Run HTPS no critic benchmark 

