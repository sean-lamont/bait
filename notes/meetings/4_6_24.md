# PhD Update 4/6/24

## Pair data
- Trained some models
  - With/without retrieved state
  - With/without open goals

- From some random samples, generated data doesn't seem that useful
  - Repeats of trivial proof cases, e.g. induction base case vs similar step case, proving with a false assumption
  - Pairing of goals with different variable names, same semantic meaning
    - Could be improved with LLMs

## Future Direction
- Have spent a long time on search, with little to show for it
  - Only real result now is subgoal separation being better
  - Still a lot to do to get results
    - HTPS replication, testing different goal models with all different search strategies
  - No guarantees or evidence so far that anything will improve upon simple BestFS

- Maybe worth moving back to fine-tuning
  - Already have seq2seq results showing improvement from synthetic data
  - Richer data to learn from
  - Only seq2seq baseline to improve upon 
  - Possibly overcomplicated it before with ILQL, should start with something simpler

- Need to read more and get a better idea of what to try next 

- Thoughts? Can keep some search experiments in background and shift focus 
 
## TODO
- Continue search experiments in background (pair evaluation, try replicating HTPS > BestFS)
- Test out simple RL on edge data. E.g. Basic PPO 
- Properly read recent papers (NVIDIA synthetic, LEGO prover, DSP) to get a better idea of direction

- Edge data RL
- Function based/tactic calls
- RAG similar proofs with large LLM 

